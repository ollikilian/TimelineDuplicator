# Pixera API Helper
Send JSON-RPC to Pixera at the configured host:port (default 192.168.178.138:1400).
Use Bash with a small Python/Node TCP client. Resource paths use forward slashes.
Clip assignment requires the learned protocol - see project memory.
